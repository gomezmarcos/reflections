How did viewing a diff between two versions of a file help you see the bug that was introduced?
it tells the line
it tells which file was modified before
How could having easy access to the entire history of a file make you a more efficient programmer in the long term?
because it hellps making better refactors
it makes easier life while working in a team
Why do you thing some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?
because even if they are in different files, they refer to an unique logic unit. / or problem solution
How can you use the commands git log and git diff to view the history of files?
I can use the log command to see all commits and then use diff to see the difference within selected commits.
How might using version control meake you more confident to make changes thay could break something?
Because I always can go back to a previous working state/commit.

